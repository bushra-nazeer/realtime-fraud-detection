# Copy to .env and adjust as needed.
MLFLOW_TRACKING_URI=file:./mlruns
MODEL_PATH=models/model.pkl

# For the real Kafka streaming path (docker compose --profile kafka up):
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
